# edit-like-git

Open editor just like Git does (pretty much a javascript version of the original [editor.c](https://github.com/git/git/blob/master/editor.c)). Uses Git configuration (if installed) and falls back to environment variables.
